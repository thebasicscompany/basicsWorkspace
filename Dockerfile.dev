FROM node:20-alpine

WORKDIR /app

# Install deps first (cached layer)
COPY package*.json ./
RUN npm install

# Copy source — volume mount overrides this in dev
COPY . .

EXPOSE 3000

CMD ["npm", "run", "dev"]
