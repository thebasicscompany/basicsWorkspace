// Phase 4 stub — workflow diff/comparison normalization
export function normalizeWorkflowForComparison(workflow: unknown): unknown {
  return workflow
}

/** Sanitize input format values before comparison/execution (pass-through stub) */
export function sanitizeInputFormat(input: unknown): unknown {
  return input
}

/** Sanitize tool configs before execution (pass-through stub) */
export function sanitizeTools(tools: unknown): unknown {
  return tools
}
