/**
 * Stub — trigger config aggregation.
 * Full implementation comes when triggers are wired in Phase 3D.
 */
export function populateTriggerFieldsFromConfig(
  _blockId: string,
  _value: unknown,
  _triggerId: string
) {
  // no-op stub
}
